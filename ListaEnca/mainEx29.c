//
// Created by edvaldo on 23/04/2024.
//

/*
#include "Lista_Enc.h"
#include "ex29.h"

//---------------------------------------------------------------

void testarUniao(LISTA *l1,LISTA *l2){
    for(int i = 0;i<5;i++){
        insertNode(i,l1);
    }
    imprimirStatus(l1);

    for(int i=3;i<10;i++){
        insertNode(i,l2);
    }
    imprimirStatus(l2);


    imprimirStatus(uniao(l1,l2));

}

//-------------------------------------------------------------

void testarInter(LISTA *l1,LISTA *l2){
    for(int i = 0;i<8;i++){
        insertNode(i,l1);
    }
    imprimirStatus(l1);

    for(int i=3;i<15;i++){
        insertNode(i,l2);
    }
    imprimirStatus(l2);

    imprimirStatus(interseccao(l1,l2));

}

void testarDife(LISTA *l1,LISTA *l2){
    for(int i = 0;i<8;i++){
        insertNode(i,l1);
    }
    imprimirStatus(l1);

    for(int i=3;i<15;i++){
        insertNode(i,l2);
    }
    imprimirStatus(l2);

    imprimirStatus(diferenca(l1,l2));

}

int main(){
    LISTA l1;
    LISTA l2;
    initializarList(&l1);
    initializarList(&l2);
    testarUniao(&l1,&l2);
    testarInter(&l1,&l2);
    testarDife(&l1,&l2);
}

*/